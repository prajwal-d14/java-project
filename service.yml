apiVersion: v1
kind: Service
metadata:
  name: my-app-service
  labels:
    app: my-app
spec:
  selector:
    app: my-app

  type: NodePort
  ports:
  - nodePort: 31032    
    port: 8080
    targetPort: 8080
